import { createApp } from "vue";
import app from "./app.vue"

//this is components, mount it to and id. to call it, simply give something the corresponding id

createApp(app).mount("#app") //to call it, <div id="app"></div>